package enums;
/**
 * Enum for type of attack roll spell caster has to make if spell calls for it
 * * NONE for no spell attack roll
 * * RANGED for a ranged spell attack
 * * MELEE for a melee spell attack
 */
public enum attckType
{
	NONE,
	RANGED,
	MELEE
}
